const images = {
   logo: require('src/assets/images/logo.svg').default,
   defaultImage: require('src/assets/images/th.jpg'),
   // no need to use .default for image which is not svg
};

export default images;
